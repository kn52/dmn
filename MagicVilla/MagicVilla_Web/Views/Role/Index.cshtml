@model IEnumerable<MagicVillaServiceJ.UserRoleDTO>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-4 border">
    <div class="row pb-2">
        <div class="text-primary">
            <h3 class="text-primary">Role List</h3>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Role" asp-action="ViewCreate" class="btn btn-primary">Create New</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedBy)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedBy)
                    </td>
                    <td>
                        @Html.ActionLink("Update", "ViewUpdate", new { controller = "Role", Id = item.Id }, new { @class = "btn btn-success" }) |
                        @Html.ActionLink("Delete", "Delete", new { controller = "Role", Id = item.Id }, new { @class = "btn btn-danger" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

